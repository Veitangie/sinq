POST ${env.BASE_URL}/guilds
Content-Type: application/json

{
    "name": "spellswords",
    "type": "combat"
}

$POST{
    if sinq.responses[2].code ~= 201 then
        sinq.test.fail("Failed to create guild")
    end
}
