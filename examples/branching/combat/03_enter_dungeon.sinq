$PRE{
    if not PLAYER_ID then error("Player ID missing") end
}
POST ${env.BASE_URL}/dungeons/join
Authorization: Bearer ${ AUTH_TOKEN }
Content-Type: application/json

{
    "player_id": ${PLAYER_ID},
    "dungeon": "goblin_cave"
}

$ASSERT{ sinq.assert.code(200, "Failed to enter dungeon") }
